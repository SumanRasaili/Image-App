import 'package:flutter/material.dart';

class CustomLoadedr extends StatelessWidget {
  const CustomLoadedr({super.key});

  @override
  Widget build(BuildContext context) {
    return const CircularProgressIndicator.adaptive();
  }
}
